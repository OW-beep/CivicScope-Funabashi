# CivicScope 船橋（civicscope-funabashi）

## 更新履歴（2026-07-26 保育園ダッシュボードの拡充）

- **[拡充] 保育園ダッシュボード（`/childcare`）に3つのセクションを追加**
  BODIK ODCS「子育て」グループ（79件）から、追加調査で見つけた候補データセットを検証し、
  使えるものを組み込みました。
  - **一時預かり利用状況**（`122041_itijiazukaririyojokyo`）：平成26年度〜令和5年度の年間利用者数推移。
    A型・B型の内訳合計が総数と一致することを検算済み。既存の`normalizePopulationSeries`
    （最大値ヒューリスティック）でそのまま自動認識できる構造だったため、専用パーサーは不要でした。
  - **病児保育利用者数**（`122041_byojibyogojihoikuriyou`）：平成27年度〜令和5年度の年間利用者数推移。
    同じく`normalizePopulationSeries`で対応。
  - **公立保育所一覧（施設詳細）**（`122041_kourituhoikuseibijoukyou`）：データセット名は「推移」ですが、
    実際の中身は年次系列ではなく、市内27の公立保育所それぞれの定員・建物構造・敷地面積・延床面積・
    増改築履歴の一覧でした。トレンドグラフではなく、既存の`SearchableTable`コンポーネントで
    検索可能な一覧として追加しています。
  - 両方の利用状況データで、令和2年度に利用者数が大きく落ち込んでいる点を明記（コロナ禍の影響が考えられる）。
  - 解説記事「childcare-guide」にも追記
- **[調査メモ]** 残りの拡充候補として、放課後ルーム入所児童数の推移（`122041_hrnyusyojidousu`、
  対象年齢が異なるため学童保育の文脈で別ダッシュボード向き）、休日保育利用者数
  （`122041_kyuujitsuhoikuriyou`）、子育て支援センター利用者数（`122041_r2centerriyosya`）、
  ファミリー・サポート・センター利用状況（`122041_r2famisaporiyo`）などが残っている
  （BODIK ODCS「子育て」グループは全4ページ・79件）。

## 更新履歴（2026-07-28 生活保護詳細データの追加・エリアマップ（イラスト）の新設）

- **[追加] 相談・開始・廃止の推移を生活保護ダッシュボード（`/welfare`）に追加**
  「(生活保護)生活保護の相談・開始・廃止の年度別推移」（`122041_seihosoudankaisihaisi`、
  作成：生活支援課）を追加。保護率データと同様、年度表記が和暦プレフィックスなしの数字のみ
  だったため静的データとして保持（datastore自体はactiveだったが、他の生活保護系データセットと
  形式を揃えるため同じ方式を採用）。開始世帯数と廃止世帯数の差し引きで、令和6年度は886世帯
  （廃止）が858世帯（開始）を上回り、差し引き28世帯の純減だったことを紹介。
  - 新規ファイル: `data/welfareFlow.js`
- **[追加・視覚的な新機能] 船橋市エリアマップ（イラスト）（`/area-map`）を新設**
  ユーザーからの依頼（「視覚的に楽しめる」3D地図・エリアマップ）を受けて追加。本格的な3D地図
  （地形データ・建物データが必要で本サイトの技術構成では現実的でない）の代わりに、
  サイトの既存デザイン言語（brass/paper/inkの技術文書・測量図トーン）を活かした
  アイソメトリック（疑似3D）イラストマップを、SVGで自作しました。
  - 3エリア（船橋駅・西船橋駅／北習志野・習志野台／南船橋・湾岸）を"台地"として立体的に表現
  - 駅マーカーの大きさは、[鉄道駅別乗車人員ダッシュボード](/rail-ridership)の実データ
    （`combineByStationName()`）に基づく正確な比率（西船橋320,152人/日が最大）
  - 梨の木・波などの装飾アイコンで、エリアの雰囲気を演出
  - 地理的な正確さより雰囲気重視のイラストである旨を明記し、誤解を招かないよう配慮
  - 新規ファイル: `components/AreaMapIllustration.jsx`, `pages/area-map.js`
  - トップページに視覚的なコールアウトを設置。3つのエリア別記事からも相互リンク

## 更新履歴（2026-07-28 水質データ・企業家計経済カテゴリの調査、保護率の追加）

- **[調査] 水質データ（司法・安全・環境カテゴリの残り）は今回見送り**
  「公共用水域水質及び底質の測定結果」を確認したところ、河川・海域の地点ごとに個別の
  Excel（.xls）ファイルが分かれて公開されている形式でした（例:「令和5年度公共用水域水質測定結果
  （要監視項目（北山ダムサイト表層））」等）。地点数が多く、かつXLS形式（本サイトのCSV/DataStore
  前提の自動取得パイプラインとは相性が悪い）のため、まとまったダッシュボードにするには
  地点ごとの個別ダウンロード・Excel解析が必要になり、今回は費用対効果の面で見送りました。
  必要であれば特定地点に絞って手動で扱うことは可能です。
- **[調査] 「企業・家計・経済」カテゴリ（gr_0700、26件）を確認**
  想定に反し、大半が生活保護関連の詳細データ（月次）でした。医療扶助人員入院・外来別推移、
  世帯類型別構成、生活保護の相談・開始・廃止の年度別推移、年齢階級別構成、扶助別構成など、
  既存の生活保護ダッシュボードを大きく拡充できる候補が複数見つかりました。今回はこのうち
  最も汎用性の高い「保護率」データを追加し、残りは今後の拡充候補としてメモしています。
  計量器検査・燃料油メーター検査等の他のデータセットは一般向けダッシュボードとしての
  優先度は低いと判断しました。
- **[追加] 保護率（船橋市・千葉県・全国比較）を生活保護ダッシュボード（`/welfare`）に追加**
  「(生活保護)被保護世帯、人員及び保護率の推移」（`122041_hihogosetaijininhogoritu`、
  作成：生活支援課）を追加。datastore未対応（datastore active: False）のCSVアップロードの
  ため、welfareHouseholds.jsと同様、値を確認の上で静的データとして保持。保護率（人口
  1,000人あたりの被保護人員）が人口・被保護人員から算出した値と一致することを平成26年度〜
  令和6年度の全11年度で検算済み。船橋市の保護率（14.34‰）は千葉県平均（13.02‰）をやや
  上回るが全国平均（16.2‰）は下回る、という水準を紹介。
  - 新規ファイル: `data/protectionRate.js`
  - welfare-households-guide記事にも追記

## 更新履歴（2026-07-28 他のBODIKカテゴリ調査・公園ダッシュボード追加）

- **[調査] BODIK ODCSの未調査カテゴリを確認**
  - 「住宅・土地・建設」（gr_0800、34件）：大半が過去の住所対照表（旧新対照表）で新規性は
    低い。分譲マンションデータ（階層別/構造別/地区別、いずれも平成30年1月1日単発スナップ
    ショット）や市営住宅申込倍率（H28/H29、古い）はあるが、時系列ダッシュボード向きではない
    と判断し見送り。
  - 「司法・安全・環境」（gr_1400、48件）：避難場所・避難所・帰宅困難者支援施設は既に活用済み。
    新たに「広場 詳細情報」「特色のある公園 詳細情報」（"いきいきふれあいマップ"）を発見し、
    今回追加。公共用水域水質測定結果（河川・海域、地点ごとに多数のデータセット）も見つかったが、
    数が多く今回は見送り（今後の候補としてメモ）。
  - 最近追加された新着データセット（2026-07-22）として、地域・職域連携推進事業実績、運動教室
    実績、糖尿病教室・健康講座実績など保健関連の実績データも複数確認（今後の候補）。
- **[追加] 公園・広場ダッシュボード（`/parks`）と解説記事**
  「広場 詳細情報」（`122041_hiroba`、令和7年度末時点、122箇所）と「特色のある公園 詳細情報」
  （`122041_tokusyoku`）を追加。緯度経度が含まれないため地図ではなく検索可能な一覧表で提供。
  合計面積・遊具の有無・開設年代別の内訳（1980年代整備が最多で33箇所）はPythonで集計・検算した
  静的データとして保持し、一覧表自体は`getDatasetRecords()`で毎回最新版を自動取得。
  - 新規ファイル: `data/parks.js`, `pages/parks.js`
  - 解説記事「parks-guide」を追加
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加

## 更新履歴（2026-07-27 将来人口推計を追加）

- **[追加] 将来人口推計データを人口ダッシュボード（`/dashboard`）に追加**
  「将来人口推計（市全体）」（`122041_shoraijinkou`、政策企画課作成、第3次総合計画策定時の
  推計）を追加しました。2008年〜2063年、5年おき12時点、年齢階級別（19区分）の内訳合計が
  総数と一致することを全時点で検算済みです。元データはワイド形式（年齢階級×年）だったため、
  年ごとの配列に変換して静的データ化。
  - 新規ファイル: `data/futurePopulation.js`
  - 人口のピークは2033年頃（約66.4万人）、2063年には約62.8万人（ピーク比-5.4%）という推計。
    生産年齢人口は減少・老年人口は倍増という年齢構成の変化もあわせて可視化。
- **[追加] 解説記事「船橋市の人口、ピークはいつ？2063年までの推計を見る」を追加**
  トップページの「よく読まれている記事」に採用（population-data-guideと入れ替え）。
  「2023年以降は参考値」という作成元の注記や、社会情勢の変化が反映されていない点を明記し、
  推計を鵜呑みにしないよう注意喚起。

## 更新履歴（2026-07-27 SEO改善・高齢化率/出生率データの追加）

- **[SEO改善] 全ページ共通のSeoコンポーネントを新設し、OGP・Twitter Card・canonical・JSON-LDを追加**
  これまでtitleとmeta descriptionのみだったため、SNSシェア時のカード表示（OGP画像なし）や、
  検索エンジン向けの構造化データが一切ない状態でした。`components/Seo.jsx`を新設し、
  全24ページ（ダッシュボード20＋静的ページ4）と記事テンプレート・記事一覧を移行しました。
  - canonical URL、og:title/description/image/url/site_name、twitter:card等を自動出力
  - 記事ページには`Article`のJSON-LD構造化データ（headline, datePublished, author等）を追加
  - `_document.js`にサイト全体の`Organization`/`WebSite` JSON-LDを追加
  - ブランドの共通OGP画像（`public/og-image.png`、1200×630）を新規作成
  - サイトマップに記事の`lastmod`（更新日）を追加
  - 既存のtitle/descriptionの文言・内部リンク構造・robots.txt/sitemap.xmlの網羅性は
    そのまま維持（変更なし、追加のみ）
- **[追加] 高齢化率データを高齢者向け住宅ダッシュボード（`/senior-housing`）に追加**
  「サービス付き高齢者向け住宅」記事が過去に「市全体の高齢化率データは見当たらなかった」と
  していた点について、実際には別データセット「65歳以上人口推移」
  （`122041_65saiijyoujinkousuii`）としてBODIK ODCSに存在することを確認済みでしたが、
  データセット側の年度表記が和暦プレフィックスなしの数字のみ（例:「12」「30」「元」）という
  特殊な形式だったため、位置ベースで平成/令和を補って静的データ化しました
  （総人口比率＝総人口・65歳以上人口からの算出値と一致することを検算済み）。
  平成12年度〜令和4年度の23年分、12.3%→24.0%という明確な右肩上がりの推移を追加。
  - 新規ファイル: `data/agingRatio.js`
- **[追加] 合計特殊出生率データを子ども・子育てダッシュボード（`/children`）に追加**
  「出生統計:合計特殊出生率の年次推移」（`122041_goukeitokusyusyusseiritu`、厚生労働省
  人口動態調査の調査票情報）を追加。元データが「区分×年度」のワイド形式だったため、
  年度ごとの配列に変換して静的データ化。船橋市・千葉県・全国の3系列を比較でき、
  全国の値が既知の公式統計（例: 令和2年全国TFR=1.33）と一致することを確認済み。
  - 新規ファイル: `data/fertilityRate.js`
- **[追加] 解説記事「船橋市の出生率と高齢化率、この20年余りの変化」を追加**
  上記2つのデータを組み合わせ、「高齢化率は23年で2倍、出生率は全国平均をやや下回る」
  という船橋市の人口構造の変化を紹介。検索流入が見込めるキーワード
  （「船橋市 高齢化率」「船橋市 出生率」）を意識した構成。

## 更新履歴（2026-07-27 エリア別記事・財政の他団体比較を追加）

- **[追加] 財政ダッシュボード（`/finance`）に「類似団体（全国の同規模自治体）との比較」セクションを追加**
  総務省「財政状況資料集」（令和6年度、船橋市公式サイトでも同内容を確認可能）をもとに、
  財政力指数・実質公債費比率・経常収支比率・将来負担比率・人口1,000人当たり職員数の
  類似団体内順位（62団体中）を追加しました。将来負担比率が62団体中1位（実質負担なし）という
  強みと、経常収支比率が53位と下位に位置するという弱みの両方を、順位付きで正直に紹介しています。
  類似団体は近隣市に限らず全国が対象ですが、このグループには柏市も含まれています。
- **[追加] 人口ダッシュボード・財政ダッシュボードのデータを使い、エリア別記事を3本追加**
  ユーザーからの依頼（「エリア別記事」「近隣市比較」）を受けて追加。特定の町丁目データではなく、
  本サイトに既にある鉄道駅別乗車人員データ（駅ごとの実数・伸び率）を軸に、船橋市を
  3エリアに分けて紹介する記事を作成しました。
  - 「船橋駅・西船橋駅エリア」（中心市街地、都心近接・商業集積）
  - 「北習志野・習志野台エリア」（新京成沿線の住宅地、東葉高速北習志野駅は5年で+31.5%）
  - 「南船橋・湾岸エリア」（商業施設と三番瀬の干潟が同居するエリア、南船橋駅は5年で+32%）
  各記事とも、良い面だけでなく「このエリアの伸びしろ」として住宅価格・交通利便性・防災上の
  留意点なども明記。
- **[調査メモ]** 保育定員・待機児童数の近隣市比較は、比較可能な公式データを短時間では
  確認できなかったため、今回は見送りました。時間をかけて千葉県または近隣各市の
  公表資料を個別に確認すれば対応可能です。

## 更新履歴（2026-07-26 魅力発信コンテンツの追加）

- **[追加] 人口ダッシュボード（`/dashboard`）に近隣市との比較セクションを追加**
  千葉県公式サイトが公表する「令和2年国勢調査－人口等基本集計結果の概要（千葉県版）」
  （千葉県総合企画部統計課、BODIK ODCSでも船橋市統計書でもなく千葉県のPDFレポート）から、
  船橋市の人口増加数の県内順位（国勢調査4回分：平成17年3位→平成22年1位→平成27年1位→
  令和2年2位）と、近隣市との人口密度比較を追加しました。
  - 新規ファイル: `data/populationComparison.js`
- **[追加] 「船橋は選ばれるまちなのか」人口増加数の近隣市比較記事**
  松戸市・市川市・柏市・習志野市・浦安市との比較を交えつつ、「大きいのに増え続けている」という
  船橋市の特徴を紹介。増加率では上位に入らない点も隠さず明記。
- **[追加] 「数字で見る、船橋というまちの伸びしろと魅力」記事**
  ユーザーからの依頼（「船橋に興味をもってもらう、住みたくなるような記事を。ただし課題点も
  惜しみなく、まろやかに」）を受けて追加。本サイトがこれまでに構築した実データ
  （鉄道アクセス・人口増加・梨畑・財政健全化・女性参画の進展）を「魅力」として、
  待機児童数の増加傾向や刑法犯認知件数の増加傾向は「伸びしろ」として、断定を避けつつ
  正直に紹介する構成。トップページの「よく読まれている記事」にも採用（evacuation-map-guideと
  入れ替え）。

## 更新履歴（2026-07-25 バス運輸状況・保育園ダッシュボード追加）

- **[追加] 市内バス運輸状況ダッシュボード（`/bus-ridership`）と解説記事**
  I-2（市内バス運輸状況）をデータ化しました。I-3（鉄道駅別乗車人員）と同様、テキスト抽出だけでは
  事業者ごとのブロック区切りが判別できなかったため、ユーザー提供のスクリーンショット（画像2枚）を
  目視で確認し、7事業者（京成バス・船橋新京成バス・ちばレインボーバス・京成トランジットバス・
  京成バスシステム・平和交通・鎌ヶ谷観光バス）それぞれの数値を突き合わせて作成しました。
  平和交通・鎌ヶ谷観光バスは1日平均乗車人員が非公表のため、合計値には含めずその旨を明記しています。
  鎌ヶ谷観光バスは令和6年度に運行終了。
  - 新規ファイル: `data/busRidership.js`, `pages/bus-ridership.js`
  - 解説記事「bus-ridership-guide」を追加
- **[追加] 保育園ダッシュボード（`/childcare`）と解説記事**
  ユーザーの依頼を受け、保育園・幼稚園関連のデータをBODIK ODCSで調査したところ、
  「子育て」グループ（79件）の中に「保育施設数及び定員数の推移」（`122041_nyusyosinseinyusyojidou`）
  という、まさに求めていた構造化データ（CSV・DataStore対応）を発見しました。
  これはPDFではなく正式なCKANデータセットのため、他の主要ダッシュボードと同じ自動取得パイプライン
  （`getDatasetRecords`）で扱えます。ただし25列という特殊な固定レイアウト
  （年度＋[施設数,定員数,入所児童数,待機数]×5区分＋合計）で、列名がShift_JIS由来の文字化けを
  起こしやすかったため、`lib/bodik.js`に専用の正規化関数`normalizeChildcareCapacity()`を追加し、
  列名パターンではなく列の「位置」で機械的に区切る方式にしました（内訳合計＝合計列であることを
  Pythonで検算済み）。
  - 新規ファイル: `pages/childcare.js`、`lib/bodik.js`に`normalizeChildcareCapacity`/
    `buildChildcareInsights`を追加、`siteConfig.js`に`datasets.childcareCapacity`を追加
  - 解説記事「childcare-guide」を追加（定員数は増えているのに待機数も増えている、という
    一見矛盾した状況を紹介）
  - 既存の「子ども・子育てダッシュボード」（`/children`）から本ダッシュボードへの導線を追加
  - BODIK ODCSの「子育て」グループには、ほかにも「公立保育所整備状況の推移」「一時預かり利用状況」
    「病児保育利用者数」「放課後ルーム入所児童数の推移」など、拡充候補となるデータセットが複数
    見つかっています（全79件、4ページ）。今後の拡充候補として記録しておきます。
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加

## 更新履歴（2026-07-25 鉄道駅別乗車人員ダッシュボード追加）

- **[追加] 鉄道駅別乗車人員ダッシュボード（`/rail-ridership`）と解説記事**
  I-3（市内鉄道駅別1日平均乗車人員）をデータ化しました。このデータは当初、
  テキスト抽出だけでは駅名と数値の対応を確実に判定できず、以前のレビューでは
  「保留」としていましたが、ユーザーから表全体が見えるスクリーンショット（画像3枚）の
  提供を受け、目視で駅名・事業者・数値を1件ずつ突き合わせることで正確に構築できました。
  市内35駅×事業者すべてを収録。西船橋・船橋・北習志野は複数事業者が同じ駅名で
  乗り入れている（乗換駅の）ため、事業者ごとに区別しつつ、駅名で合算した集計
  （`combineByStationName()`）も提供しています。
  - 新規ファイル: `data/railRidership.js`, `pages/rail-ridership.js`
  - 解説記事「rail-ridership-guide」を追加（西船橋駅が合算で圧倒的1位、船橋日大前駅の
    伸び率+86%など）。トップページの「よく読まれている記事」にも採用
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加

## 更新履歴（2026-07-24 雇用・市民相談ダッシュボード追加、町会自治会に経年推移を追加）

- **[追加] 雇用・求人ダッシュボード（`/employment`）と解説記事**
  L-1（一般職業紹介状況）から、有効求人倍率・新規求人数・新規求職申込件数の推移を可視化。
  船橋公共職業安定所管内（船橋市・習志野市・八千代市・鎌ケ谷市・白井市）の数値である旨を明記。
- **[追加] 市民相談ダッシュボード（`/citizen-consultation`）と解説記事**
  O-1（市民相談状況）から、市民相談（法律・生活）と市民の要望等の件数推移・内訳を可視化。
  内訳合計が総数と一致することを検算済み。5年で総数が約19%減少している点を、断定を避けつつ紹介。
- **[追加] 町会・自治会ダッシュボード（`/chokai`）に、船橋市統計書O-11による5年間の加入世帯数推移を追加**
  既存のBODIK ODCS由来データは現時点のスナップショットのみだったため、経年変化を補足。
  町会数は増加している一方、加入世帯数は減少しているという対比を明記。
  あわせてchokai-directory-guide記事にも言及を追加。
  - 新規ファイル: `data/employment.js`, `pages/employment.js`, `data/citizenConsultation.js`,
    `pages/citizen-consultation.js`, `data/chokaiTrend.js`
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加

## 更新履歴（2026-07-24 女性参画ダッシュボード追加）

- **[追加] 女性参画ダッシュボード（`/gender-participation`）と解説記事「船橋市の女性参画、この5年の変化」を追加**
  ユーザーからアップロードいただいた船橋市統計書PDF（`24O.pdf`＝O章「市民生活」）のうち、
  O-7（女性の政策決定機関への参加状況）・O-8（男女共同参画センター利用状況）をデータ化しました。
  議員数・委員数から算出した比率が統計書記載の比率(%)と一致すること、男女共同参画センターの
  相談業務内訳の合計が総数と一致することを検算済みです。
  - 新規ファイル: `data/genderParticipation.js`, `pages/gender-participation.js`
  - 解説記事「gender-participation-guide」を追加
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加
- 同時にL章（労働）・O章（市民生活）から検算済みの8表をCSV化（L-1有効求人倍率、L-6勤労市民センター、
  O-1市民相談状況、O-5情報公開制度、O-7女性参画、O-9/O-10自転車駐車場・撤去、O-11町会自治会）。
  ダッシュボード化していない表はユーザー提供分として別途CSVで納品。

## 更新履歴（2026-07-24 財政ダッシュボード追加）

- **[追加] 財政ダッシュボード（`/finance`）と解説記事「船橋市の財政を『家計』に例えると」を追加**
  ユーザーからアップロードいただいた船橋市統計書PDF（`27R.pdf`＝R章「財政」）のうち、
  R-1（会計別歳入決算及び予算）・R-2（会計別歳出決算及び予算）・R-3（市債の現在高）を
  Pythonで検算（内訳合計＝一般会計、普通債＋その他債＝一般会計 等）した上でCSV化・データ化しました。
  R章は多段見出し・結合セルが多く、以前は「転記リスクが高いため見送り」としていましたが、
  今回チェックサム方式で正確に復元できることが確認できたため、正式に取り込みました。
  - 新規ファイル: `data/finance.js`, `pages/finance.js`
  - 解説記事「finance-household-budget-guide」を追加（歳出・歳入の内訳を月30万円の家計に換算する内容）
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加
- **[修正] 記事本文レンダラー（`pages/articles/[slug].js`）に箇条書き・リンク記法のサポートを追加**
  以前は「- 項目」のような箇条書きが改行を保持できず、ブラウザ上で1行の文章に潰れて表示される
  バグがありました（`food-business-directory-guide`記事に既存のバグとして存在）。`renderBody`を
  改修し、「- 」で始まる行が連続するブロックは`<ul><li>`として、`[text](url)`はリンクとして
  正しく描画されるようにしました。

## 更新履歴（2026-07-24 回遊率改善）

- **[追加] ダッシュボード⇔解説記事の相互リンクを全ページに追加**
  すべてのダッシュボードページ（人口・子ども子育て・学校・高齢者向け住宅・生活保護・治安救急・
  町会自治会・食品営業施設・生活衛生施設・防災）の末尾に、対応する解説記事への導線と
  「ほかのダッシュボード一覧」への導線をセットで表示する`DashboardFooterLinks`コンポーネントを追加。
  記事ページ側にも、対応するダッシュボードへの導線（`article.relatedDashboard`）と、
  「あわせて読みたい」関連記事（同タグ優先、`getRelatedArticles()`）を追加しました。
- **[追加] トップページに「新着記事」「よく読まれている記事」をダッシュボード一覧より上に配置**
  以前は解説記事セクションがページ下部（ダッシュボード一覧・豆知識の後）にありましたが、
  回遊率向上のため、統計スナップショットの直後・ダッシュボード一覧より前に移動しました。
  - 「新着記事」：`getArticlesSortedByDate()`による日付降順（実装済み）
  - 「よく読まれている記事」：**注意** 本サイトにはまだアクセス解析基盤が無く、実際のPVに基づく
    「人気記事」ランキングは実装していません。代わりに`articles.js`内で`featured: true`を付けた
    記事を編集部おすすめとして表示しています（`getFeaturedArticles()`）。実データに基づくランキングに
    したい場合は、Vercel AnalyticsやGoogle Analytics等を導入し、`getFeaturedArticles()`を
    実データ参照に置き換えることを想定しています。

## 更新履歴（2026-07-24 新ダッシュボード追加）

- **[追加] 治安・救急ダッシュボード（`/public-safety`）を新規追加**
  ユーザーからアップロードいただいた船橋市統計書PDF（`26Q.pdf`＝Q章「警察及び消防」、
  `27R.pdf`＝R章「財政」）のうち、抽出精度を検算で確認できたQ章の一部を、
  `welfareHouseholds.js`と同じ「静的JSデータ＋出典明記」方式でCSV化・データ化しました。
  - Q-1 刑法犯の罪種別認知件数（資料：千葉県警察本部）
  - Q-11 救急出動件数（資料：消防局）
  それぞれ令和2年～令和6年の内訳合計が統計書記載の総数と一致することをPythonで検算済みです。
  R章（財政）は多段見出し・結合セルが多くテキスト抽出だけでは列対応の確度が低いため、
  今回は見送っています（`/mnt/user-data/outputs/funabashi_toukeisho_csv/`に変換済みCSVあり）。
  - 新規ファイル: `data/publicSafety.js`, `pages/public-safety.js`
  - ヘッダー・フッター・サイトマップ・トップページのダッシュボード一覧にリンクを追加
  - 解説記事「public-safety-dashboard-guide」を追加

## 更新履歴（2026-07-23 追加対応）

- **[修正・重要] 学校ダッシュボードが常にエラーになっていた根本原因を修正**
  前回「IDを差し替えれば直る」としていましたが、実際には新IDのデータセットは
  「学校ごとの生徒数一覧」ではなく「市内の中学校生徒数の合計を年度ごとに集計した時系列データ」
  （学校名にあたる列が存在しない）でした。ランキング表示を前提にした
  `normalizeNameValueList` では列を認識できず、エラーが出続けていました。
  `pages/schools.js` を、人口ダッシュボードと同じ時系列の正規化・グラフ表示に作り直し、
  実際のデータ構造に合わせています。あわせて、per-school比較を前提にしていた
  「junior-high-school-students-guide」記事の内容も、実際のデータ（市内合計の長期推移）に
  即した内容に書き換えました。
- **[追加] AED設置施設データを発見・反映**
  以前は「船橋市の場合、AEDデータはBODIK ODCSには無く、別カタログ（G空間情報センター）に
  掲載されているがロボット除け設定で確認できない」としていましたが、実際には
  `https://data.bodik.jp/dataset/122041_aed_sikoukyousisetsu` としてBODIK ODCS上に存在することを確認し、
  防災ダッシュボードのAEDレイヤーを有効化しました（緯度・経度列を含む標準フォーマットのため、
  既存の施設マップの仕組みがそのまま使えました）。
- **[追加] 公衆無線LAN（Wi-Fi）アクセスポイント一覧を追加**
  `https://data.bodik.jp/dataset/122041_public_wireless_lan` を防災ダッシュボードの新レイヤーとして追加しました。
  平時の利便性に加え、災害時の通信手段としても有用です。

## 更新履歴（2026-07-23 レビュー対応）

- **[修正] 学校ダッシュボードのデータセットIDが古く404になっていた問題を修正**
  `data/siteConfig.js` の `datasets.schoolStudents.id` を `122041_h300501_chugakkouseitosu`（現在のカタログに存在しない旧ID）から、
  現行の `122041_chugakkouseitosuu` に修正しました。放置すると `/schools` が常にエラー表示になります。
- **[修正] 「サービス付き高齢者向け住宅」記事の事実誤認・自己矛盾を修正**
  同記事は「高齢化率・生活保護のデータは見当たらなかった」としていましたが、実際には
  ①高齢化率（総人口比率）を収録した別データセット（`122041_65saiijyoujinkousuii`）が存在し、
  ②生活保護データはサイト内の別記事ですでに扱っていました。誤った記述を修正し、他記事への導線に差し替えています。
- **[修正] 解説記事の日付が公開想定日より先の日付（未来日付）になっていた点を修正**
  「生活保護世帯データ」記事の日付を1日前倒し。
- **[改善] 記事一覧・トップページの表示順を新着順（日付降順）に変更**
  以前は配列の記述順（＝古い記事が先頭）のまま表示されていました。`getArticlesSortedByDate()` を追加し、
  トップページは最新3件＋「すべての記事を見る」導線、`/articles` は全件を新着順で表示するようにしています。
- **[追加] 今後の記事・ダッシュボード向けに、追加調査で見つけたデータセットIDを`siteConfig.js`に登録**
  高齢化率、60歳以上人口推移、合計特殊出生率、将来人口推計、国勢調査詳細集計（統計書B-2、20リソース）など。
  詳細は本READMEの「6. 追加調査で見つかった有望なデータセット」を参照してください。
- **[セキュリティ] Next.jsを14.2.5→14.2.35に更新**
  14.2.5は2025年12月に複数のCVE（ミドルウェア認可バイパー等）が報告されたバージョンのため、
  当時点での最新パッチ版に更新しました。デプロイ前に `npm install` を再実行してください。
- **[追加] Aboutページに運営者プロフィールのセクションを追加**

船橋市のオープンデータカタログ（[BODIK ODCS](https://odcs.bodik.jp/122041/)）から
「常住人口」「町会・自治会一覧」などのデータをAPI経由で自動取得し、
グラフ・検索表・解説記事として発信するNext.js製サイトです。Vercelでのホスティングを前提にしています。

- サイト名: CivicScope Funabashi（シビックスコープ船橋）
- お問い合わせ: civicscope.funabashi@gmail.com
- データ出典: 船橋市オープンデータカタログ（CC BY 4.0）

---

## 1. ブラウザだけでVercelにデプロイする手順

ローカルにNode.jsやgitを入れなくても、ブラウザだけで以下の手順でデプロイできます。

### Step 1: GitHubにリポジトリを作る
1. https://github.com にログイン（アカウントがなければ作成）。
2. 右上の「+」→「New repository」。リポジトリ名は `civicscope-funabashi` など任意。Public/Privateどちらでも可。「Create repository」をクリック。

### Step 2: このフォルダの中身をアップロードする
1. 作成したリポジトリのページで「uploading an existing file」というリンク（または「Add file」→「Upload files」）をクリック。
2. お渡ししたzipを解凍し、**中のファイル・フォルダをすべて**（`pages`や`components`フォルダごと）ドラッグ＆ドロップでアップロード。
3. 一番下の「Commit changes」をクリックして保存。

   ※ フォルダごとドラッグ＆ドロップすればGitHub側でフォルダ構造を保ったままアップロードされます。うまくいかない場合は、フォルダを1つずつ順番にアップロードしてください。

### Step 3: Vercelでプロジェクトを作る
1. https://vercel.com にログイン（GitHubアカウントでログイン可）。
2. 「Add New...」→「Project」。
3. 先ほどのGitHubリポジトリを選択して「Import」。
4. Framework Presetは自動的に「Next.js」が検出されます。特に設定変更は不要です。
5. 「Environment Variables」の欄は、最初は空のままでOKです（下記3章参照）。
6. 「Deploy」をクリック。数分でビルドが完了し、`https://（プロジェクト名）.vercel.app` のURLでサイトが公開されます。

### Step 4: 独自ドメインを取るとき（あとで／任意）
サイトURLは `https://civic-scope-funabashi.vercel.app` で確定済みです（`data/siteConfig.js`の`url`と`public/robots.txt`にすでに反映済み）。検索流入などが見込めるようになったタイミングで独自ドメインを取得したら、この2箇所を書き換えてGitHub上でCommitしてください。Vercelは自動的に再ビルド・再デプロイします。

---

## 2. データセットの差し替え・追加方法

`data/siteConfig.js` の `datasets` に、BODIK ODCSのデータセットID（データセットURLの末尾、例: `122041_maitukijoujyu`）を追加するだけで、`lib/bodik.js` の共通関数がAPI経由で自動的にデータを取得します。CSVの列名を直接指定する必要はありません（列名は正規表現でヒューリスティックに推測しています）。

新しいデータを使ったページを増やしたい場合は、`pages/chokai.js` を参考に、`getDatasetRecords("データセットID")` を`getStaticProps`内で呼び出してください。

### 町丁目別の分布マップについて

`pages/chokai.js` と `pages/food-businesses.js` は、住所らしき列（`所在地`・`住所`・`町丁目`など）を自動検出し、
[Geolonia 住所データ](https://github.com/geolonia/japanese-addresses)（無料・CC BY 4.0）を使って町丁目名から緯度経度を引き当て、
[Geolonia が公開する行政境界データ](https://github.com/geolonia/japanese-admins)（国土交通省「国土数値情報」をもとに作成）を
背景の輪郭として使い、実際の船橋市域の上にバブルを正確な縮尺でプロットしています。

住所らしき列が見つからない、またはマッチ率が低い場合は、`地区`のような、より粗いカテゴリ列での
ランキング表示に自動でフォールバックします（`pages/chokai.js`はこの二段構えになっており、
どちらも使えない場合のみ一覧表だけの表示になります）。

### 防災ダッシュボード（複数レイヤーの地点マップ）について

`pages/disaster-prevention.js`は、避難場所・避難所・帰宅困難者支援施設のデータセットに
緯度経度の列（`緯度`・`経度`など）が含まれていればそれを使い、施設1件＝地図上の1点として
船橋市の行政境界の上に正確な位置でプロットします（`lib/geo.js`の`guessLatLngFields`/`extractPointsFromLatLng`、
描画は`components/FacilityMap.jsx`）。データセットごとにレイヤーとして重ね合わせ、
チェックボックスで表示・非表示を切り替えられます。同じ仕組みは、緯度経度を持つ
他の施設データを追加する際にも再利用できます（`data/siteConfig.js`の`datasets`に追加するだけです）。

「区分」のような分類列が「1」「不明」のようなコードで読み取りにくい場合に備えて、
`guessCategoryField`は実際の値を見て、数字コードだけの列は後回しにするようにしています。
また、避難場所・避難所データは「洪水」「地震」「津波」のように災害種別ごとの対応可否フラグを
持つ形式（内閣官房の指定緊急避難場所データ標準に準拠）であることが多いため、
`buildHazardBreakdown`でこれらのフラグ列を自動検出し、災害種別ごとの対応件数として
集計・グラフ化しています（単一のコード化された「区分」列より読み取りやすい切り口です）。

**AEDデータについて**：船橋市の場合、AED設置施設一覧はBODIK ODCS（data.bodik.jp）ではなく
「G空間情報センター」という別カタログに掲載されており、そのサイトはロボット除け設定のため
自動で正確なデータセットIDを確認できませんでした。`data/siteConfig.js`の`datasets.aed`に
`id`（データセットのURL末尾）を設定すれば、既存の仕組みでそのまま取り込めます
（`apiBase`も忘れずに設定してください）。IDが空の間、防災ダッシュボードにはAEDレイヤーが
表示されず、その旨の案内と船橋市公式のAEDマップへのリンクが表示されます。

**地図の細かさについて**：現在の地図は国土数値情報（国土交通省）による市全体の行政境界のみを
使っており、町丁目や番地ごとの細かい境界線・クリックで色を塗るような塗り分け表示（コロプレス図）は
対応していません。町丁目単位の詳細な境界ポリゴンデータ（e-Statの小地域境界データなど）を
別途用意できれば同様の仕組みで実装できますが、無料で確実に使える出典を確認できていないため、
今回は見送っています。該当するデータソースが見つかれば教えてください。

---

## 3. Google AdSense審査に向けたチェックリスト

コードは審査を通りやすいように以下を満たす形で用意していますが、申請前に確認・準備してください。

- [x] プライバシーポリシー（`/privacy`）: 広告配信・Cookieについて明記済み
- [x] 利用規約・データ出典（`/terms`）
- [x] 運営者情報・お問い合わせ（`/about`, `/contact`）
- [x] オリジナルの解説記事（`/articles` 配下に5本。今後増やすほど有利です）
- [x] AdSenseクライアントID（`ca-pub-4630812027939211`）を`data/siteConfig.js`に設定し、`pages/_document.js`のHead内に静的な`<script>`タグとして直接埋め込み済み（すべてのページの初期HTMLに含まれます）
- [x] Google Search Console用の`google-site-verification`メタタグを`pages/_document.js`に設定済み
- [x] `public/ads.txt`にpublisher ID（`pub-4630812027939211`）を設定済み
- [ ] **審査に通るまでは広告枠は非表示**にしてあります（`data/siteConfig.js`の`adsEnabled: false`）。審査確認用の`<script>`タグ（`_document.js`）自体は常に出力されますが、`AdSlot`コンポーネントは`adsEnabled`が`false`の間は何も描画しません。**審査に通ったら`adsEnabled`を`true`に書き換えてください。**
- [ ] **独自ドメインの用意を推奨**（`.vercel.app`のままでも審査自体は可能ですが、独自ドメインの方が信頼性の面で有利です。検索流入が見込めると判断した時点で取得し、`data/siteConfig.js`の`url`と`public/robots.txt`のSitemap行を書き換えてください）
- [ ] **公開後、ある程度の期間・記事数を積み上げてから申請**するのが一般的です（Googleは低品質・薄いコンテンツを理由に却下することが多いため、記事を10本前後に増やしてからの申請をおすすめします）
- [ ] 審査通過後、各広告枠（`AdSlot`コンポーネント）の表示位置をAdSense管理画面の自動広告設定に任せるか、
      個別のスロットIDを環境変数（`NEXT_PUBLIC_ADSENSE_SLOT_HOME` など、`pages/*.js`内で参照している変数名）として設定する

別のAdSenseアカウント・クライアントIDに差し替えたい場合は、`data/siteConfig.js`の`adsensePublisherId`を書き換えてください（`pages/_document.js`が自動的にこの値を使います）。

---

## 4. ローカルで動作確認したい場合（任意）

Node.js 18以上がある環境で以下を実行すると、ブラウザで `http://localhost:3000` から確認できます（この手順は必須ではありません。Step1〜3のブラウザのみの手順でも問題なく公開できます）。

```bash
npm install
npm run dev
```

---

## 6. 追加調査で見つかった有望なデータセット（次に追加するなら）

`https://data.bodik.jp/dataset/122041_stats_b-2`（船橋市統計書 B-2「国勢調査」）は、
1つのデータセットの中に**20個のリソース（CSV）**がまとまった、かなり濃いページです。
すでに`data/siteConfig.js`に`censusHouseholdComposition`として登録済みですが、
`getDatasetRecords()`は「最初のリソース」しか見ないため、実際に使うには
`getPackage("122041_stats_b-2")`でresources一覧を取得し、用途に応じて`resource_id`を
指定して`getDatastoreRecords()`を呼ぶ形に拡張する必要があります。中でも特に有望なのは：

| リソース | 見出し・切り口の例 | SEO/バズ度 |
| --- | --- | --- |
| B-2-19 昼間人口 | 「船橋市は夜になると人口が減る"通勤都市"？昼間人口と夜間人口の差を可視化」。都心通勤者の多さを裏付ける定番ネタ。 | ◎（検索・SNS両方で強い） |
| B-2-7 年齢(5歳階級)、男女別高齢単身者数 | 「高齢単身者、この数字は増えている？」。孤独死・見守り施策との関連で読まれやすい。 | ◎ |
| B-2-1 年齢（各歳）男女別人口 | 各歳ピラミッドを描ける唯一のデータ。「船橋市の"人口ボリュームゾーン"は何歳？」 | ◎ |
| B-2-20 流出・流入先別15歳以上就業者数及び通学者数 | 「船橋市民はどこへ通勤・通学しているか」。東京都心/都内区への流出先ランキングは地元民の関心が高い。 | ◎ |
| B-2-9 / B-2-10 母子・父子世帯数 | ひとり親世帯の推移。福祉・子育て文脈で検索流入が見込める。 | ○ |
| B-2-3 世帯人員別一般世帯数 | 単身世帯化の進行度。生活保護記事と同じ切り口を全世帯で見せられる。 | ○ |
| B-2-13〜17 産業・職業別就業者数 | 「船橋市はどんな産業で働く人が多い街か」。企業誘致・移住検討層に刺さる。 | ○ |

このほか、単独データセットとして次のものも見つかっています（すでに`siteConfig.js`にID登録済み）：

- **65歳以上人口推移**（`122041_65saiijyoujinkousuii`）：総人口比率＝高齢化率をそのまま含む、まさに
  「サービス付き高齢者向け住宅」記事が探して見つからなかったと書いていたデータです。単独記事化の優先度は高めです。
- **出生統計:合計特殊出生率の年次推移**（`122041_goukeitokusyusyusseiritu`）：全国・千葉県との比較表つき。
  「船橋市の出生率は全国平均より高いか低いか」という比較記事は検索流入が見込めます。
- **将来人口推計（市全体）**（`122041_shoraijinkou`）：現状データだけでなく将来推計も扱えると、
  「このまま人口は増え続けるのか」という未来志向の記事が書けて独自性が上がります。

いずれも生活保護世帯データと同様、行数が少なく更新頻度も低いため、`welfareHouseholds.js`と同じ
「値を確認した上で静的JSデータとして埋め込む」方式が実装コストと更新の手間のバランスが良いと思います。

## ディレクトリ構成

```
civicscope-funabashi/
├─ pages/            # 各ページ（Next.js Pages Router）
│  ├─ index.js        トップページ
│  ├─ dashboard.js     人口ダッシュボード
│  ├─ chokai.js        町会・自治会検索
│  ├─ articles/        解説記事一覧・詳細
│  ├─ about.js / contact.js / privacy.js / terms.js
│  └─ sitemap.xml.js
├─ components/       # UIパーツ
├─ lib/bodik.js      # BODIK ODCS(CKAN) APIクライアント・データ正規化
├─ data/
│  ├─ siteConfig.js   サイト設定・データセットID一覧
│  └─ articles.js     解説記事の本文
├─ styles/globals.css
└─ public/           # robots.txt, ads.txt, favicon.svg
```
